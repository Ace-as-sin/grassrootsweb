import React from 'react';
import './Hero.scss';

const Hero = () => {
    return (
        <div className="hero">
            {/* Your background image goes here */}
        </div>
    );
};

export default Hero;